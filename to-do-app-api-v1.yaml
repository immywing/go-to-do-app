swagger: "2.0"
info:
  description: "To Do App"
  version: "1.0.0"
  title: "To Do App"
host: "localhost:8081"
basePath: "/"
tags:
- name: "ToDos"
  description: "Everything to manage your ToDos"
schemes:
- "http"
paths:
  /todo:
    post:
      tags:
      - "ToDos"
      summary: "Add a new ToDo"
      description: "Add a ToDo to the store"
      operationId: "addToDo"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - in: "body"
        name: "body"
        description: "ToDo object that needs to be added to the store"
        required: true
        schema:
          $ref: "#/definitions/ToDo"
      responses:
        "400":
          description: "Invalid input"
        "422":
          description: "Validation exception"
    put:
      tags:
      - "ToDos"
      summary: "Add or Update an existing ToDo"
      description: "Add or Update a ToDo in the store"
      operationId: "updateToDo"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - in: "body"
        name: "body"
        description: "ToDo object that needs to be added or updated"
        required: true
        schema:
          $ref: "#/definitions/ToDo"
      responses:
        "400":
          description: "Invalid ID supplied"
        "404":
          description: "ToDo not found"
        "422":
          description: "Validation exception"
    get:
      tags:
      - "ToDos"
      summary: "Get a ToDo by ID"
      description: "Retrieve a specific ToDo by its ID"
      operationId: "getToDo"
      produces:
      - "application/json"
      parameters:
      - name: "id"
        in: "query"
        description: "ID of the ToDo to retrieve"
        required: true
        type: "string"
        format: "uuid"
      - name: "user_id"
        in: "query"
        description: "ID of the user associated with the ToDo"
        required: true
        type: "string"
        format: "uuid"
      responses:
        "200":
          description: "Successful response"
          schema:
            $ref: "#/definitions/ToDo"
        "400":
          description: "Invalid ID supplied"
        "404":
          description: "ToDo not found"
definitions:
  ToDo:
    type: "object"
    required:
    - "id"
    - "user_id"
    - "title"
    - "priority"
    - "complete"
    properties:
      id:
        type: "string"
        format: "uuid"
      user_id:
        type: "string"
        format: "uuid"
        description: "ID of the user associated with the ToDo"
      title:
        type: "string"
        example: "Complete ToDo App"
      priority:
        type: "string"
        description: "Priority of the ToDo"
        enum:
        - "Low"
        - "Medium"
        - "High"
        default: "Medium"
      complete:
        type: "boolean"
        default: false
externalDocs:
  description: "Find out more about Swagger"
  url: "http://swagger.io"
